---
import DocsLayout from '../layouts/DocsLayout.astro';
---

<DocsLayout title="发布与供应链安全" description="每个安装包都可校验来源、完整性和依赖清单。" section="Release security">
  <Fragment slot="toc">
    <a href="#assets">Release 内容</a>
    <a href="#checksum">Checksum</a>
    <a href="#provenance">签名与来源</a>
    <a href="#report">安全报告</a>
  </Fragment>

  <h2 id="assets">Release 内容</h2>
  <ul>
    <li>macOS PKG 与 tar.gz。</li>
    <li>Linux DEB、RPM 与 tar.gz。</li>
    <li>Windows Setup EXE 与 zip。</li>
    <li>`SHA256SUMS`、CycloneDX SBOM 和 Sigstore Bundle。</li>
  </ul>

  <h2 id="checksum">校验 SHA-256</h2>
  <pre><code># Linux
sha256sum -c SHA256SUMS --ignore-missing

# macOS
shasum -a 256 -c SHA256SUMS</code></pre>
  <p>Windows 可使用 `Get-FileHash`，将输出与 `SHA256SUMS` 对比。</p>

  <h2 id="provenance">签名与构建来源</h2>
  <p>Release 工作流使用 GitHub OIDC 生成无密钥 Sigstore 签名，并为安装包生成构建来源证明。验证时应同时检查仓库、工作流身份和 Tag。</p>

  <h2 id="report">安全问题</h2>
  <p>不要在公开 Issue 中粘贴 Token、服务地址中的秘密参数或未披露漏洞。请按照仓库 <a href="https://github.com/dijkstra402/emoera-RAG-CLI/blob/main/SECURITY.md">SECURITY.md</a> 私下报告。</p>
</DocsLayout>

---
import DocsLayout from '../layouts/DocsLayout.astro';
---

<DocsLayout title="故障排查" description="从配置、认证、能力和服务状态四层快速定位问题。" section="Operations">
  <Fragment slot="toc">
    <a href="#baseline">基础诊断</a>
    <a href="#auth">认证与权限</a>
    <a href="#network">网络与超时</a>
    <a href="#install">安装问题</a>
  </Fragment>

  <h2 id="baseline">基础诊断</h2>
  <pre><code>emoera config list
emoera auth status
emoera capabilities
emoera status
emoera --debug whoami</code></pre>
  <p>Debug 输出用于诊断请求地址、阶段和错误分类，但不会主动打印 Token。</p>

  <h2 id="auth">认证与权限</h2>
  <h3>401 / Token 无效</h3>
  <p>重新执行 `emoera auth set-token`，确认 Token 未过期、撤销或复制不完整。</p>
  <h3>403 / 没有权限</h3>
  <p>检查 Token Scope、账号组织权限和文档公开范围。CLI 不会绕过服务端数据隔离。</p>

  <h2 id="network">网络与超时</h2>
  <p>先运行 `emoera status`。对大型问答可以提高超时：</p>
  <pre><code>emoera --timeout 180s ask "生成完整对比报告"</code></pre>
  <p>若流式回答末尾出现 EOF，但正文已经完整，升级到最新版本；新版会正确识别服务端完成事件。</p>

  <h2 id="install">安装问题</h2>
  <ul>
    <li>安装后命令不存在：关闭并重新打开终端。</li>
    <li>macOS 拦截：在“隐私与安全性”允许该安装包。</li>
    <li>Windows SmartScreen：先校验 SHA-256，再选择继续运行。</li>
    <li>Linux 无 Keychain：在无桌面环境使用 `EMOERA_API_TOKEN`。</li>
  </ul>
</DocsLayout>
